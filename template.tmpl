// Code generated by "github.com/maru44/bqgen"; DO NOT EDIT.

package out // TODO fix

import (
    "github.com/maru44/bqgen/core"

    "cloud.google.com/go/bigquery"
)

type {{ $.ID | camel }} struct {
    {{ range $f := $.Fields -}}
        {{ if and (ne $f.Type "RECORD") (ne $f.Type "STRUCT") -}}
            {{ $f.Name | camel }} {{ $f.Type | gt }} `json:"{{ $f.Name }}" toml:"{{ $f.Name }}" yaml:"{{ $f.Name }}"`
        {{ end -}}
    {{ end -}}
}

var {{ $.ID | camel }}Schema = &core.Table{
    ID: "{{ $.ID }}",
    DatasetID: "{{ $.DatasetID }}",
    Schema: bigquery.Schema{
        {{ range $f := $.Fields -}}
            { {{ $f | pf }} },
        {{ end -}}
    },
    TypeByFieldName: map[string]bigquery.FieldType{
        {{ range $f := $.Fields -}}
            "{{ $f.Name }}": {{ $f.Type | pt }},
        {{ end -}}
    },
}

var {{ $.ID | camel }}Columns = struct {
    {{ range $f := $.Fields -}}
        {{ $f.Name | camel }} string
    {{ end -}}
} {
    {{ range $f := $.Fields -}}
        {{ $f.Name | camel }}: "{{ $f.Name }}",
    {{ end -}}
}
